import {createSlice} from '@reduxjs/toolkit';
import {uuid} from '@reduxjs/toolkit'

const initialState = [
  
]

const frontPageSlice = createSlice({
    name:'frontPage',
    initialState,
    reducers:{

    }

})

export default frontPageSlice.reducer